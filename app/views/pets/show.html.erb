<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300&display=swap" rel="stylesheet">
  <title>Pet Profile</title>
</head>
<body>
  <div class="container">





     <h1><%= @pet.name%></h1>
    <div class="user-profile-card">
      <div class="user-profile-photo">
        <% if @user.present? && @user.profile_photo.attached? %>
          <%= cl_image_tag @user.profile_photo.key, class: "profile-photo" %>
        <% else %>
          <div class="default-profile-photo">No Profile Photo Available</div>
        <% end %>
      </div>

<% if @pet.photo.attached? %>
  <%= cl_image_tag @pet.photo.key, height: 270, style: "border-radius: 10px;" %>
<% else %>
  <div class="placeholder-image" style="border-radius: 10px;">No Pet Photo Available</div>
<% end %>



  <p> Story: <%= @pet.story %></p>
<p> Species: <%= @pet.species %></p>
<p>Breed: <%= @pet.breed %></p>
<p> Age: <%= @pet.age %> years</p>
<p> Found on: <%= @pet.found_when.strftime('%B %d, %Y') %></p>
<p> Status: <%= @pet.status %></p>
  </div>

<%= form_with(model: @pet_adoption, local: true) do |form| %>
  <div class="form-container">
    <div class="form-group">
      <%= form.label :your_name, 'Your Name' %>
      <%= form.text_field :your_name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :email, 'Email' %>
      <%= form.email_field :email, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :reason_for_adoption, 'Reason for Adoption' %>
      <%= form.text_area :reason_for_adoption, class: 'form-control' %>
    </div>

    <div class="form-actions">
      <%= form.submit 'Adopt Me', class: 'btn btn-primary' %>
      <%= link_to 'Back', root_path, class: 'btn btn-secondary' %>
      <%= link_to 'Delete', pet_path(@pet), 'data-turbo-method': :delete, data: { confirm: 'Are you sure you want to delete this pet?' }, class: 'btn btn-danger' %>
    </div>
  </div>
<% end %>


    
</html>
