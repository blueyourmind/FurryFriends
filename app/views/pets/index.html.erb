   <div class="table-container">
      <% @pets.each do |pet| %>
        <div class="pet-card">
          <h2><%= pet.name %></h2>
          <% if pet.photo.attached? %>
            <%= link_to pet_path(pet) do %>
              <%= cl_image_tag pet.photo.key, height: 140 %>
            <% end %>
          <% else %>
            <div class="placeholder-image">No Photo Available</div>
          <% end %>
          <p>Breed: <%= pet.breed %></p>
          <p>Age:<%= pet.age %> years</p>
        </div>
      <% end %>
    </div>
